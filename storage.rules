rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      // Public can view images
      allow read: if true;
      // Only authenticated admin can upload/delete images
      allow write: if request.auth != null;
    }
  }
}
